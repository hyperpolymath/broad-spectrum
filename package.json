{
  "name": "broad-spectrum",
  "version": "1.0.0",
  "private": true,
  "description": "ReScript build dependencies only - runtime uses Deno",
  "scripts": {
    "build": "rescript build",
    "clean": "rescript clean",
    "watch": "rescript build -w"
  },
  "devDependencies": {
    "@rescript/core": "^1.5.0",
    "gentype": "^4.5.0",
    "rescript": "^11.0.1"
  }
}
